# Deployment Guide - Netlify

This guide will help you deploy Piyusha's UI/UX Designer Portfolio to Netlify.

## 🚀 Quick Deploy to Netlify

### Method 1: Direct Netlify Deploy (Recommended)

1. **Push to GitHub** (if not already done):
   ```bash
   git add .
   git commit -m "Production ready: Piyusha UI/UX Portfolio"
   git push origin main
   ```

2. **Deploy to Netlify**:
   - Go to [netlify.com](https://netlify.com)
   - Click "Add new site" → "Import an existing project"
   - Connect your GitHub account
   - Select your repository
   - Use these settings:

### Netlify Build Settings

| Setting | Value |
|---------|-------|
| **Base directory** | `frontend` |
| **Build command** | `npm run build` |
| **Publish directory** | `out` |
| **Node version** | `20` |

### Environment Variables (Optional)

Add these in Netlify Dashboard → Site Settings → Environment Variables:

| Variable | Value | Description |
|----------|-------|-------------|
| `NEXT_PUBLIC_SITE_URL` | `https://your-site-name.netlify.app` | Your site URL |
| `NEXT_PUBLIC_CONTACT_EMAIL` | `hello@piyusha.design` | Contact email |
| `NEXT_PUBLIC_ANALYTICS_ID` | (optional) | Google Analytics ID |

### Method 2: Manual Deploy

1. **Build locally**:
   ```bash
   cd frontend
   npm install
   npm run build
   ```

2. **Deploy the `out` folder**:
   - Go to [netlify.com/drop](https://netlify.com/drop)
   - Drag and drop the `frontend/out` folder
   - Your site will be live instantly!

## 📋 Pre-Deployment Checklist

- ✅ Build completes successfully (`npm run build`)
- ✅ All images load properly
- ✅ 3D gallery works in production
- ✅ Navigation between pages works
- ✅ Contact email is updated
- ✅ Social media links are updated
- ✅ Meta tags and SEO are configured

## 🔧 Custom Domain Setup (Optional)

1. **Buy a domain** (recommended: `piyusha.design` or `piyushadesign.com`)
2. **In Netlify Dashboard**:
   - Go to Site Settings → Domain Management
   - Click "Add custom domain"
   - Follow DNS configuration instructions

## 🎯 Performance Optimizations

The site is already optimized with:
- ✅ Static site generation
- ✅ Image optimization
- ✅ Code splitting
- ✅ Caching headers
- ✅ Minified assets
- ✅ WebGL fallbacks

## 🔍 SEO Configuration

Update these for better SEO:
- Site title: "Piyusha - UI/UX Designer"
- Meta description: "Explore Piyusha's UI/UX design portfolio..."
- Open Graph images (add to `public/` folder)
- Sitemap (auto-generated by Next.js)

## 📱 Testing Checklist

Before going live, test:
- [ ] Desktop browsers (Chrome, Firefox, Safari, Edge)
- [ ] Mobile devices (iOS Safari, Android Chrome)
- [ ] 3D gallery performance on different devices
- [ ] Page load speeds
- [ ] Contact form functionality
- [ ] All internal links work

## 🚨 Troubleshooting

### Build Fails
- Check Node.js version (should be 18+)
- Run `npm install` in frontend directory
- Check for TypeScript errors

### 3D Gallery Not Working
- Ensure WebGL is supported
- Check browser console for errors
- Verify Three.js assets are loading

### Images Not Loading
- Check image URLs are accessible
- Verify images are optimized
- Check network tab in browser dev tools

## 📊 Analytics Setup (Optional)

1. **Google Analytics**:
   - Create GA4 property
   - Add tracking ID to environment variables
   - Update layout.tsx with tracking code

2. **Netlify Analytics**:
   - Enable in Netlify Dashboard
   - View traffic and performance metrics

## 🔄 Continuous Deployment

Once connected to GitHub:
- Every push to `main` branch triggers automatic deployment
- Preview deployments for pull requests
- Rollback capability in Netlify Dashboard

## 📞 Support

If you encounter issues:
1. Check Netlify build logs
2. Test build locally first
3. Check browser console for errors
4. Verify all dependencies are installed

---

**Ready to launch Piyusha's amazing portfolio! 🎨✨**